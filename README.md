# Discord Changelog Website

A static HTML/CSS/JS changelog builder with:
- Glassmorphism UI
- Live preview (title, description, datetime, color)
- Discord webhook integration

## Run
Just open `index.html` in a browser.

## Discord webhook
Set the webhook URL in the UI (it won't be stored automatically). Click **Send to Discord** to post the current entry.

